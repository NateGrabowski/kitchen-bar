# Suggested Commands

## Development
```bash
npm run dev          # Start Vite dev server at http://localhost:5173
npm run build        # Build for production
npm run preview      # Preview production build
```

## System Commands (Windows via Git Bash)
```bash
ls                   # List directory contents
find . -name "*.jsx" # Find files
grep -r "pattern"    # Search in files
pwsh -Command '...'  # PowerShell 7 (single-quote $variables)
```

## Git
```bash
git status           # Check working tree status
git add .            # Stage all changes
git commit -m "msg"  # Commit changes
git log --oneline    # View commit history
```

## Screenshots (via Playwright skill)
```bash
cd C:/Users/nateg/.claude/plugins/cache/playwright-skill/playwright-skill/4.1.0/skills/playwright-skill
node run.js /tmp/your-script.js
```
